# K-뷰티 Phase 1: UI 배치 및 제품 매칭 규칙 설계

> 이 문서는 **Phase 1** 기준으로,  
> ① 어느 스텝에 어떤 블록을 넣을지(UI 위치),  
> ② 톤·컬러 태그와 제품 매칭 규칙  
> 을 구체화합니다.

---

## 1. Phase 1 UI 배치

### 1-1. 배치 위치 (단일 선택)

| 옵션 | 위치 | 설명 |
|------|------|------|
| **채택** | **Step 5 (AI 분석 결과) 내부, result-grid** | 퍼스널 컬러·추천 스타일 결과가 나온 직후, 같은 화면에서 "이 톤에 어울리는 K-뷰티" 노출. 사용자 맥락이 가장 명확함. |
| 대안 B | Step 4 하단 (컬러 분석 입력 직후) | 피부톤·언더톤만 있고 퍼스널 컬러(봄/여름/가을/겨울)는 아직 없어, 추천 품질이 떨어짐. |
| 대안 C | Step 6 (Virtual Try-On) 상단 | 코디 시연 후 메이크업 제품 추천으로 이어질 수 있으나, Phase 1에서는 분석 결과 직후가 구현이 단순함. |

**결정: Step 5 `#analysis-result` 안 `.result-grid` 에 새 카드 1개 추가.**

---

### 1-2. DOM 구조 (삽입 위치)

**부모**: `#analysis-result .result-grid`

**삽입 순서**:  
기존 순서를 유지하면서, **「추천 스타일」 카드(`.style-card`) 바로 다음**에 삽입.

```
.result-grid
├── .result-card.personal-color-card     (퍼스널 컬러)
├── .result-card.style-card              (추천 스타일)
├── .result-card.k-beauty-card   ← ★ NEW (K-뷰티 추천)
├── #mood-music-result                   (나만의 무드 & 음악)
├── .result-card.recommendation-card     (코디 추천)
├── .result-card.tips-card               (스타일링 팁)
└── .result-card.fashion-image-card      (AI 패션 이미지, span 2)
```

**새 카드 권장 마크업 (Phase 1)**:

```html
<div class="result-card k-beauty-card" id="k-beauty-recommendation">
  <div class="card-header">
    <span class="card-icon">💄</span>
    <h4>이 톤에 어울리는 K-뷰티</h4>
  </div>
  <div class="k-beauty-content" id="k-beauty-content">
    <p class="k-beauty-lead">당신의 퍼스널 컬러에 맞는 제품을 추천해요</p>
    <div class="k-beauty-slots" id="k-beauty-slots">
      <!-- JS: 제품 카드 1~2개 삽입 (Phase 1: 매칭 규칙으로 선택) -->
    </div>
    <p class="k-beauty-disclaimer">추천 · 일부 링크는 제휴/광고일 수 있어요</p>
  </div>
</div>
```

**제품 카드 1개당 (슬롯)**:

```html
<a class="k-beauty-slot" href="{product.url}" target="_blank" rel="noopener noreferrer">
  <img class="k-beauty-slot-img" src="{product.image}" alt="">
  <div class="k-beauty-slot-info">
    <span class="k-beauty-slot-brand">{product.brand}</span>
    <span class="k-beauty-slot-name">{product.name}</span>
    <span class="k-beauty-slot-tone">{product.toneLabel}</span>
  </div>
</a>
```

---

### 1-3. 표시 시점

- **표시**: `#analysis-result`를 보여줄 때(`display: block`) 동시에 표시.
- **데이터**: AI 분석 결과 `result.personalColor`(시즌·설명·팔레트)와, 기 입력값 `stylingData.skinTone`, `stylingData.undertone`을 사용해 매칭된 제품 1~2개를 채움.
- **Phase 1**: 분석 결과 렌더링하는 기존 JS(예: `personalColorEl.innerHTML = ...` 하는 블록) **바로 다음**에, `#k-beauty-content` / `#k-beauty-slots`를 채우는 로직 한 번 호출.

---

### 1-4. CSS 클래스·레이아웃 요약

| 클래스 | 용도 |
|--------|------|
| `.result-card.k-beauty-card` | 기존 result-card와 동일한 카드 스타일 재사용 |
| `.k-beauty-content` | 카드 본문 영역 |
| `.k-beauty-lead` | 상단 문구 "당신의 퍼스널 컬러에 맞는 제품을 추천해요" |
| `.k-beauty-slots` | 제품 슬롯들을 감싸는 flex/grid 컨테이너 (가로 1~2개) |
| `.k-beauty-slot` | 제품 1개 (이미지 + 브랜드/이름/톤라벨) |
| `.k-beauty-disclaimer` | "추천 · 일부 링크는 제휴/광고일 수 있어요" |

- 그리드 레이아웃: `.result-grid`가 이미 grid라면, 새 카드는 **한 셀**로 두고, 내부 `.k-beauty-slots`만 `display: flex; gap: 12px;` 등으로 1~2열 배치하면 됨.

---

## 2. 제품 매칭 규칙 (톤·컬러 태그)

### 2-1. 사용자 측 입력 (매칭 입력)

| 키 | 출처 | 값 (예시) | 용도 |
|----|------|-----------|------|
| **season** | `result.personalColor.season` | `"봄웜"`, `"여름쿨"`, `"가을웜"`, `"겨울쿨"` 등 | 퍼스널 컬러 계절 → 제품 `season` 태그와 매칭 |
| **undertone** | `stylingData.undertone` | `warm` \| `cool` \| `neutral` | 제품 `undertone` 태그와 매칭 (동점 시 보조) |
| **skinTone** | `stylingData.skinTone` | `fair` \| `light` \| `medium` \| `tan` | 베이스 제품(쿠션·파운데이션) 색상 추천 시 사용 (Phase 2에서 확장) |

- **Phase 1 매칭 입력 단일화**: **1차 기준은 AI 분석 결과 `result.personalColor.season`만 사용.** `undertone`은 1순위 매칭 후 동점 시 보조로 사용. (Step 4 사용자 선택과 AI season 불일치 방지를 위해 AI 결과를 우선.)
- season은 파싱 시 **봄→spring, 여름→summer, 가을→autumn, 겨울→winter** 로 정규화해서 사용.

---

### 2-2. 제품 측 태그 (톤·컬러 태그)

제품 메타데이터에 아래 태그를 부여한다고 가정.

| 태그 키 | 값 (예시) | 설명 |
|---------|-----------|------|
| **season** | `spring` \| `summer` \| `autumn` \| `winter` | 이 제품 색상이 잘 어울리는 퍼스널 컬러 시즌 (복수 가능) |
| **undertone** | `warm` \| `cool` \| `neutral` | 어울리는 언더톤. `neutral`이면 웜/쿨 모두 노출 가능 |
| **skinTone** | `fair` \| `light` \| `medium` \| `tan` | 주로 베이스 제품용. 해당 피부톤에 맞는 색상 |
| **category** | `lip` \| `blush` \| `foundation` \| `cushion` \| `eyeshadow` \| `etc` | Phase 2에서 카테고리별 필터/우선순위용 |

**Phase 1 최소 스키마 (제품 1개)**:

```json
{
  "id": "product-1",
  "name": "라벤더 립 틴트",
  "brand": "브랜드A",
  "image": "https://...",
  "url": "https://...",
  "season": ["summer"],
  "undertone": "cool",
  "toneLabel": "여름쿨 톤에 어울려요"
}
```

- **toneLabel**: 카드에 그대로 노출하는 짧은 문구 (예: "여름쿨 톤에 어울려요", "웜톤 추천").

---

### 2-3. Phase 1 매칭 로직 (단순 규칙)

**입력**:  
- `userSeason`: `spring` | `summer` | `autumn` | `winter` (result.personalColor.season에서 파싱)  
- `userUndertone`: `warm` | `cool` | `neutral`

**규칙 (우선순위)**:

1. **1순위**: `product.season` 배열에 `userSeason` 포함 **그리고**  
   `(product.undertone === userUndertone || product.undertone === 'neutral')`  
   → 최대 2개 선택 (없으면 2순위로).
2. **2순위**: `product.undertone === userUndertone || product.undertone === 'neutral'` 만 만족  
   → 최대 2개 선택 (없으면 3순위).
3. **3순위**: 아무 제품이나 2개 (또는 1개) fallback.

**동점일 때**:  
- `category` 순서 lip → blush → foundation 등 고정해서 1~2개만 노출 (Phase 1에서는 고정 순서로 처리해도 됨).

**매칭 0건·fallback**  
- **0건**: 카드 내에 "이 톤에 맞는 제품을 준비 중이에요. 곧 더 많은 제품을 만나보세요." 문구만 표시. (선택) CTA 또는 "인기 K-뷰티 보기" 링크 1개.
- **fallback**: 3순위에서 노출할 때는 `undertone === 'neutral'` 제품만 사용하거나, 고정 "인기 제품" 1개만 노출해 톤 무관 노출을 최소화.

**Phase 1 구현 팁**:  
- 제품 목록을 JSON/JS 배열로 두고, 위 규칙으로 필터 후 `slice(0, 2)`로 2개만 채워 넣으면 됨.

---

### 2-4. 시즌·언더톤 매핑 (한글 → 태그)

| result.personalColor.season (예시) | userSeason |
|------------------------------------|------------|
| 봄, 봄웜, 봄 웜 | `spring` |
| 여름, 여름쿨, 여름 쿨 | `summer` |
| 가을, 가을웜, 가을 웜 | `autumn` |
| 겨울, 겨울쿨, 겨울 쿨 | `winter` |

- 문자열에 "봄" 포함 → spring, "여름" → summer, "가을" → autumn, "겨울" → winter 로 매핑하면 됨.

---

## 3. Phase 1용 예시 제품 데이터 (하드코딩용)

아래는 매칭 규칙 테스트 및 Phase 1 UI 채우기용 **최소 예시**입니다. 실제로는 제휴 링크·이미지 URL로 교체하면 됨.

```json
[
  {
    "id": "p1",
    "name": "소프트 라벤더 립",
    "brand": "K-Beauty A",
    "image": "/image/k-beauty/placeholder-lip.jpg",
    "url": "https://example.com/product1",
    "season": ["summer"],
    "undertone": "cool",
    "toneLabel": "여름쿨 톤에 어울려요"
  },
  {
    "id": "p2",
    "name": "로즈 블러셔",
    "brand": "K-Beauty B",
    "image": "/image/k-beauty/placeholder-blush.jpg",
    "url": "https://example.com/product2",
    "season": ["summer", "spring"],
    "undertone": "neutral",
    "toneLabel": "웜·쿨 모두 무난해요"
  },
  {
    "id": "p3",
    "name": "코랄 립 틴트",
    "brand": "K-Beauty C",
    "image": "/image/k-beauty/placeholder-lip2.jpg",
    "url": "https://example.com/product3",
    "season": ["spring", "autumn"],
    "undertone": "warm",
    "toneLabel": "봄·가을 웜톤 추천"
  },
  {
    "id": "p4",
    "name": "딥 레드 립",
    "brand": "K-Beauty D",
    "image": "/image/k-beauty/placeholder-lip3.jpg",
    "url": "https://example.com/product4",
    "season": ["winter", "autumn"],
    "undertone": "cool",
    "toneLabel": "겨울쿨·가을 톤"
  }
]
```

- Phase 1에서는 위 4개만 있어도, 시즌/언더톤 조합별로 1~2개씩 노출 가능.

**제품 DB 운영 (보완)**  
- 관리 주체와 갱신 주기(예: 분기 1회)를 정해 두고, 링크·이미지 만료 시 해당 제품 숨김 또는 대체 제품으로 교체하는 규칙을 두는 것을 권장. 상세는 `K-BEAUTY-전문가-분석-및-개선안.md` 참고.

---

## 4. 구현 체크리스트 (Phase 1)

- [ ] **HTML**: `#analysis-result .result-grid` 안, `.style-card` 다음에 `.result-card.k-beauty-card` 블록 추가 (또는 JS로 동적 삽입).
- [ ] **CSS**: `.k-beauty-card`, `.k-beauty-slots`, `.k-beauty-slot` 스타일 추가 (기존 result-card와 톤 맞춤).
- [ ] **데이터**: 제품 배열(JSON 또는 JS 상수) 정의, 각 제품에 `season`, `undertone`, `toneLabel` 포함.
- [ ] **매칭**: `userSeason`(season 한글→영어), `userUndertone` 사용해 2-3절 규칙으로 1~2개 제품 선택.
- [ ] **렌더링**: AI 분석 결과 표시 시점에 `#k-beauty-slots`에 선택된 제품 카드 1~2개 채우기.
- [ ] **Disclaimer**: "추천 · 일부 링크는 제휴/광고일 수 있어요" 문구 노출.
- [ ] **0건 처리**: 매칭 결과 0건일 때 안내 문구(및 선택 CTA) 표시.
- [ ] **매칭 입력**: AI `result.personalColor.season` 우선 사용, `undertone` 보조.

이대로 적용하면 Phase 1 UI 위치와 제품 매칭 규칙을 그대로 개발에 사용할 수 있습니다.  
Phase 2에서는 `skinTone`, `category`(스킨케어 포함), 추천 점수(스코어) 확장을 추가하면 됩니다.  
**문제점·보완점·제안점** 전체는 `K-BEAUTY-전문가-분석-및-개선안.md` 참고.
