# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Environment variables (업로드 제외)
.env
.env.local
.env.*
!.env.example

# Generated config (contains API keys from .env)
config.js

# Dependencies
node_modules/

# Build outputs (npm run build → dist/) — 배포 시 Cloudflare가 빌드로 생성
dist/
build/

# Temporary files
*.tmp
*.temp

# Large media (GitHub 100MB limit). soave1.mp4(11MB)는 저장소에 포함
movie/*.mp4
movie/*.webm
!movie/soave1.mp4

# BGM 배포용 — music/ 포함 (푸시·빌드 시 dist에 복사됨)
# music/ 은 제외하지 않음 (추적·배포 대상)

# 한글 system 임시 생성물 (갤러리·ani·information은 푸시 대상)
image/hangeul/system/temp/
